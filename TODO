TODO: Fix load more button re-rendering entire grid instead of just rendering new cards. This works sometimes, but it's pretty rare. Either has something to do with GameCard key changing/duplicating when fetching more data, or some state change causing the whole data.map code block to re-run
